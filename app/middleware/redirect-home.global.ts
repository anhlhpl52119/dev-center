export default defineNuxtRouteMiddleware((to, from) => {
  // console.log('hit from', from.path);
  // console.log('hit to', to.path);
  //   const localePath = useLocalePath();

//   if (to.path === '/en') {
//     return navigateTo(localePath('/web/etc/Generator'), { replace: true });
//   }
});
